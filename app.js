/**
 * Pseudocode
 */

 // Need to prompt user for information about team manager then info about team members
    // Need to define roles
        // Use manager, engineer, intern, main
    // Need to create role specific questions, once user enters role, the resulting questions should be specific to that role
    // Prompt user for email id, and role specific info
 // Need to require inquirer
 // Need to create HTML page
 // Need to style HTML page
 // Need to create html template specific to each role type



